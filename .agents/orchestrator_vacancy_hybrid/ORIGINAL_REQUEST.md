# Original User Request

## 2026-07-17T23:15:09+09:00

동탄 테크노밸리 평균 공실률 추정 알고리즘을 국토부 실거래가 데이터, 국민연금(NPS) 고용 데이터, 건물 연면적(GFA) 및 준공 연식 데이터를 종합적으로 반영하는 다요소 하이브리드 모델로 고도화합니다.

Working directory: c:\Users\ocs56\OneDrive\바탕 화면\PORTFOLIO\PORTFOLIO - DVIEW\frontend
Integrity mode: development

## Requirements

### R1. 면적별 가중치(Tx Weight) 및 연면적(GFA) 반영 공식 고도화
- 실거래 건수 집계 시 면적 크기에 단순 구간형 가중치 대신 연속형 스케일링 함수를 도입하여 실질 공간 점유 비중을 정교화합니다.
- 건물별 총 연면적(GFA)에 따른 집적 효과 가중치를 정밀 튜닝하여 대형 지식산업센터의 임차 안정성과 공실률 수렴 속도를 차등화합니다.

### R2. 국민연금(NPS) 고용 데이터 기반 매크로 보정치 고도화
- `nps_stats.json`에 정의된 근로자 수, 기업 수, 신규 입사 및 퇴사자 추이를 다각도로 분석하여 고용 성장 속도와 순고용 증가율이 반영된 거시 보정치(`macroBonus`) 산출식을 개선합니다.

### R3. 건물 연식(준공 연도) 및 시계열 자연 퇴거율(Dynamic Turnover) 모델 정교화
- 지식산업센터별 준공 연도 데이터를 기반으로 신축 초기(예: 준공 1~2년 내 입주 속도 가속화 구간)와 기축/구축의 자연 퇴거율(Dynamic Turnover) 및 자연 공실 수렴 한계를 차별적으로 적용하는 시계열 감쇄 알고리즘을 반영합니다.

### R4. 이상치 필터링 및 거래량 부족 시 보완 폴백(Fallback) 알고리즘 강화
- 특정 시점의 거래 건수가 아예 없거나 극히 적을 때 직전 공실률 추세를 안정적으로 유지할 수 있는 평활화(예: EMA 또는 이동평균 기법) 알고리즘을 연동합니다.
- 비정상적으로 크거나 작은 면적/임대료 실거래 건을 필터링하는 이상치 제어 장치를 도입합니다.

### R5. 독립적 단위 테스트(Unit Test) 구축 및 빌드 무결성 유지
- 수정된 추정 알고리즘을 독립적으로 검증하기 위해 모크 데이터를 주입하여 다양한 엣지 케이스(거래량 0인 시기, NPS 고용 지표 급변 시기, 연식 및 연면적 차이에 따른 상이한 공실 개선 추이 등)가 정상 작동하는지 검증하는 단위 테스트를 작성합니다.
- Next.js API 라우트(`frontend/src/app/api/technovalley/trend/route.ts`)에 구현된 전체 흐름과 데이터 반환 포맷을 기존 대시보드 UI가 정상 작동하도록 하위 호환성을 완벽히 보장하여 빌드 및 린트 오류가 없도록 합니다.

## Acceptance Criteria

### Algorithm & Tests
- [ ] 신규 알고리즘 검증을 위한 단위 테스트 파일(`*.test.ts`)이 추가 또는 업데이트되어야 하며, Jest 테스트 실행(`npm run test`) 결과가 100% 통과해야 합니다.
- [ ] 거래량이 아예 없는 경우에도 추정 공실률이 튀지 않고 기존 트렌드와 평활화된 값으로 수렴하며 최소 공실률(2.0%) 하한선이 지켜져야 합니다.
- [ ] 신축 지산(예: 실리콘앨리 등)과 구축 지산의 초기 입주 가속도 차이와 자연 퇴거율 감쇄 모델이 정상 반영되어 시계열 공실 데이터가 논리적으로 산출되어야 합니다.

### Backward Compatibility & Build
- [ ] `/api/technovalley/trend` 호출 시 응답하는 JSON 데이터 구조가 기존 차트 렌더링에 정의된 Key 구성('금강 IX', '실리콘앨리', '평균공실률' 등)과 완벽히 일치하여 UI 차트가 정상적으로 노출되어야 합니다.
- [ ] `npm run audit` 또는 빌드 및 린트 파이프라인이 경고나 에러 없이 성공적으로 완료되어야 합니다.
