/**
 * @module contexts
 * @description Canonical React Context providers for application state (Auth, Settings).
 * Architecture Layer: Application / Context Layer
 */

export * from './AuthContext';
export * from './SettingsContext';
